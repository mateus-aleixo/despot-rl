[package]
name = "despot_core"
version = "0.1.0"
edition = "2021"

[lib]
name = "despot_core"
crate-type = ["cdylib"]

[dependencies]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
